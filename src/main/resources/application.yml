server:
  port: 8080
spring:
  datasource:
    url: jdbc:mariadb://proddatabase-url/prod-db-name
    username: ''
    password: ''
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: false